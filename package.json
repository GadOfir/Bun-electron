{
  "name": "your-project-name",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "bun build src --out-dir dist"
  },
  "dependencies": {
    "electron": "^latest-version"
  }
}
